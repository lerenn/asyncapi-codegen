# Issue: https://github.com/TheSadlig/asyncapi-codegen/issues/135

# Channels path should be generated as constants

# This test makes sure that it does.

asyncapi: 2.6.0
info:
  title: Sample App
  version: 1.2.3
channels:
  v2.issue135.resource:
    publish:
      message:
        payload:
          type: string
  v2.issue135.info:
    publish:
      message:
        payload:
          type: string
  v2.issue135.status:
    subscribe:
      message:
        payload:
          type: string
  v2.issue135.project:
    publish:
      message:
        payload:
          type: string
  v2.issue135.group:
    publish:
      message:
        payload:
          type: string