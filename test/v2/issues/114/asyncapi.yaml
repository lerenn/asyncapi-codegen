# Issue: https://github.com/TheSadlig/asyncapi-codegen/issues/114

# AsyncAPI document version should be generated.

# This test makes sure that it does.

asyncapi: 2.6.0
info:
  title: Sample App
  version: 1.2.3
channels:
  v2.issue114.status:
    subscribe:
      message:
        payload:
          type: string
