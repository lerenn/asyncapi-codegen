# Issue: https://github.com/lerenn/asyncapi-codegen/issues/101

# Context modification were not passed between middlewares

asyncapi: 2.6.0
info:
  title: Test101 application
  version: '1.0.0'
channels:
  v2.issue101.test:
    publish:
      message:
        payload:
          type: string
