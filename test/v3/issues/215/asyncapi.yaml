asyncapi: 3.0.0
info:
  title: Types-only generation must not contain controller boilerplate
  version: 1.0.0
channels:
  test:
    address: v3.issue215.test
    messages:
      testMessage:
        $ref: '#/components/messages/testMessage'
operations:
  testOperation:
    action: send
    channel:
      $ref: '#/channels/test'
    messages:
      - $ref: '#/channels/test/messages/testMessage'
components:
  messages:
    testMessage:
      payload:
        type: object
        properties:
          event:
            type: string
