openapi: 3.0.0

components:
  schemas:
    object:
      type: object
      properties:
        data:
          type: object
          properties:
            hello:
              type: string
            world:
              type: string
      