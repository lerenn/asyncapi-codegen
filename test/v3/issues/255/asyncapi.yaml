asyncapi: 3.0.0
info:
  title: Sample App
  version: 1.2.3

components:
  messages:
    TEST:
      payload:
        type: object
        properties:
          DateProp:
            type: string
            format: date
          DateTimeProp:
            type: string
            format: date-time