asyncapi: 3.0.0
info:
  title: date-time payload preserving fractional seconds
  version: 1.0.0
channels:
  test:
    address: v3.issue233.test
    messages:
      testMessage:
        $ref: '#/components/messages/testMessage'
operations:
  testOperation:
    action: send
    channel:
      $ref: '#/channels/test'
    messages:
      - $ref: '#/channels/test/messages/testMessage'
components:
  messages:
    testMessage:
      payload:
        type: string
        format: date-time
