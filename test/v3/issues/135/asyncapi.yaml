# Issue: https://github.com/lerenn/asyncapi-codegen/issues/135

# Channels path should be generated as constants

# This test makes sure that it does.

asyncapi: 3.0.0
info:
  title: Sample App
  version: 1.2.3
channels:
  resource:
    address: v3.issue135.resource
  info:
    address: v3.issue135.info
  status:
    address: v3.issue135.status
  project:
    address: v3.issue135.project
  group:
    address: v3.issue135.group